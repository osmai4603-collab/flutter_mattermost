name: osm_network
description: "A reusable HTTP and WebSocket networking library for Dart and Flutter projects."
version: 1.0.0

environment:
  sdk: ^3.12.2
  flutter: ">=3.19.0"

dependencies:
  flutter:
    sdk: flutter
  dio: ^5.4.3+1
  connectivity_plus: ^6.0.3
  web_socket_channel: ^3.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
