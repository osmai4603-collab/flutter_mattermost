name: osm_ui_components
description: "مكتبة ومكونات بناء الواجهات المشتركة (Shared UI Components) لمشاريع Flutter المستحواة من تطبيق Mattermost Webapp."
version: 1.0.0

environment:
  sdk: ^3.12.2
  flutter: ">=3.19.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
