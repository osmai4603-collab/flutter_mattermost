name: reusable_calls
description: A standalone, modular WebRTC calling library for Flutter applications.
version: 1.0.0
homepage: https://github.com/reusable/reusable_calls

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.10.0'

dependencies:
  flutter:
    sdk: flutter

  # WebRTC & Media Dependencies
  flutter_webrtc: ^0.12.5
  wakelock_plus: ^1.2.8
  proximity_sensor: ^1.2.9
  audioplayers: ^6.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
