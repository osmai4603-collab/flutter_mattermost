name: reusable_network
description: A standalone, modular, and project-agnostic HTTP & WebSocket network client library for Flutter.
version: 1.0.0
homepage: https://github.com/reusable/reusable_network

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.10.0'

dependencies:
  flutter:
    sdk: flutter

  # Networking & Connectivity Dependencies
  dio: ^5.4.3+1
  web_socket_channel: ^3.0.0
  connectivity_plus: ^6.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
